```cpp
#pragma once
#include <cstdint>

namespace PointType {
    enum PointTypeEnum : uint8_t {
        noLabel = 0,
        chaos = 1,
        pipe = 2,
        plane = 3,
        hypothetical = 128  // Bit 7 voor doorgetrokken aanname (bijv. pipe | hypothetical)
    };
}
```
